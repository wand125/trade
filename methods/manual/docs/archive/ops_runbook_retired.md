# 運用手順のアーカイブ(失効済み)

失効した委任・対応規定。**現在の運用には使わない** — 生きている手順は `../ops_runbook.md`。

## 失効した対応表の行(2026-08-08 削除)

| 対象 | 失効理由 |
|---|---|
| XAUUSD注文89835497(作戦02 Buy Limit) | 2026-08-06 決済済み |
| USDJPY ticket 89812483/89812489(作戦01第2次) | 2026-08-06 決済済み(−14,680円) |
| POSITIONS 2→1 のトレーリング規定 | 作戦01の規定。作戦終了につき失効 |
| LEVEL_UP 158.10 / LEVEL_DOWN 157.15 | 作戦01の地図。作戦終了につき失効 |

---

## 作戦06の執行 — **失効済み(2026-08-07 22:35、本人決定)**

**この節の委任は終了した。作戦06に基づく設置・撤廃は一切行わない。** 発動条件は一度も揃わず、設置ゼロ・約定ゼロで終わっている。settle_watch.py を起動している場合は**停止してよい**。

**現在、運用セッションに委任されている執行はゼロ件**。ポジション・注文ともになし。監視・報告のみを行い、**新規エントリーは本人の明示指示があるまで実行しない**(平常の原則へ復帰)。

以下は失効した枠の記録(再設計の参照用)。

### (失効)作戦06 の内容

全文は `methods/manual/docs/campaign.md` の「作戦06」節。**必ず読んでから執行すること**。要点:

- **委任されているのは stop 注文の「設置」のみ**。成行エントリー・ロット変更・逆張りはすべて委任外
- 対象: **USDJPY-m の戻り売り(sell_stop、20ロット)** / **XAUUSD-m の押し目買い(buy_stop、0.3ロット)**
- **各銘柄1回まで**。約定・失効・撤去のいずれでも枠を使い切る
- 同時保有は**1銘柄1玉**。両方同時に条件成立したら**先に揃った方だけ**設置し、他方は本人確認へ回す

### 発動条件(4つすべて満たしたときのみ設置)

1. ATR14 が **USDJPY 7.0pips以下 / XAUUSD 3.5ドル以下**
2. 現値が **EMA20 から ATR 1本ぶん以内**
3. EMA10 < EMA20 < EMA50(売り) / EMA10 > EMA20 > EMA50(買い) の**並びが崩れていない**
4. **設置直前に価格を再取得**し、entry と現値の距離が **ATR 0.5本以上**あること

### 設置時の水準の決め方

- SL: **その瞬間の ATR14 の1.5倍以上**、かつ直近の構造(高値/安値)の外側
- TP: **RR 1:1.2 以上**が取れる水準。取れなければ**設置しない**
- **必ず SL/TP を同時に付けて発注する**(サーバー側SLが最後の防衛線)

### 撤廃条件 → 置いた注文を削除、未設置なら作戦06を失効させる

| # | 条件 |
|---|---|
| A | EMA の並びが崩れた |
| B | USDJPY が **157.74 上抜け** / XAUUSD が **4,350.97 割れ**(指標の反応が否定された) |
| C | ATR が判定値の1.5倍超に再拡大 |
| D | USDJPY が **156.674 割れ**(介入警戒圏) |
| E | **8/8 06:00 JST 到達** |
| F | 本人の中止指示 |

撤廃を実行したら時刻・理由とともに本人へ報告する。

### 監視スクリプト(相談セッションから引き継ぎ。**runtime_watch.py とは別に起動する**)

`/private/tmp/claude-501/-Users-HHosono-Files-0-Group-trade/c53470bc-6ad5-4ea8-aeb8-b0134effd785/scratchpad/settle_watch.py`

発動条件と撤廃条件A〜Eを20秒周期で判定し、状態遷移の瞬間だけ `ARMED` / `CALM` / `KILL-A`〜`KILL-E` を出力する。**このパスはセッション固有のscratchpadなので、消えていたら campaign.md の条件表から作り直すか、条件を手で判定する**。

## 作戦13: USDJPY 箱の上抜け買い(**失効理由: 2026-08-11 09:42 無効化条件④成立により撤去、未約定・約定ゼロ・損益ゼロ**)

全文は `archive/campaign_closed.md` の「作戦13」節。作戦11は2026-08-11に置き換えで終了(未エントリー)。

### 委任されていたもの(失効)

| 対象 | 委任 |
|---|---|
| 159.40 buy_stop の設置 | 確認なしで実行してよい(実行済み、order 90204917) |
| 159.80 到達での決済 | 確認なしで実行してよい |
| 口先介入・実弾介入の観測時の撤退 | 確認なしで実行してよい |
| CPI前(8/12 21:30)の注文撤去 | 確認なしで実行してよい |
| 増ロット・ナンピン・SLの引き下げ | 委任外(いかなる場合も) |

### 発注内容(実行済み)

```
python3 src/bridge/create_trade_command.py buy_stop --symbol USDJPY-m \
  --volume 40 --price 159.40 --sl 159.14 --tp 159.75 \
  --expires-in-seconds 150 --reason "campaign13 box breakout" --live --confirm LIVE
```

### 監視していた水準

| 水準 | 意味 |
|---|---|
| 159.40 | buy_stop の約定 |
| 159.14 | SL(サーバー側で自動執行) |
| 159.75 | TP(サーバー側で自動執行) |
| 159.80 | 撤退ライン |

### 経過

08:58・09:40にUSDJPY-mが159.14を2度下抜け(159.125→159.089)、箱下限159.16へ戻りきれず。無効化条件④「箱を下抜けて戻れない」が成立し、相談セッションが本人承認のもと09:42にcancel(pending order deleted、retcode 10009)。約定ゼロ・損益ゼロ。
