# Issue(未処理タスク)

**将来的にはタスク管理を別マシン(office-work-dc、M4環境)へ移す構想。それまでの暫定としてこのファイルで管理する(2026-08-16 本人決定)。**

`claude/proposals.md` との違い: あちらは**設計・ルールの提案**(Pxx、議論して着地させるもの)。ここは**実行すれば終わる作業**(誰が・いつ・何をすれば閉じるかが決まっているもの)。

閉じたら「状態」を **完了** にし、日付と結果を1行残す。溜まったらアーカイブへ。

| ID | 内容 | 実行できる主体 | 引き金・期限 | 状態 |
|---|---|---|---|---|
| **I1** | **金チャートのEA入力 `InpCodexMaxLot` が 0.3ロットを許す値か確認**。ソース既定は **0.01**(EA 40行目)で、8/11のMac→Windows移行では実際に0.01へ戻っており0.1ロットのdry-runが「volume outside allowed range」で拒否された。**0.01のままなら来週の金の作戦は発注段階で止まる**。あわせて `InpPollCodexTradeCommands`=true と `InpRequestEverySeconds` の現在値(5秒に設定済みか30秒か)も確認 | **本人のみ**(Windows の MetaTrader GUI 操作。M4 Mac・WSL側の運用セッションからは不可) | **金の作戦で発注する前**。月曜オープン(8/17 07:00 JST)後 | **未処理・最優先** |
| **I2** | **EAのコンパイル状態を確定させる**。配備済み `.mq5` が 8/12 11:50、`.ex5` が **8/12 12:06 とソースより新しい**ため**コンパイル済みの公算が高い**が、`.ex5` は圧縮されており中身を検証できない(`strings` で文字列抽出不可を確認済み)。**確証は実地**: ブリッジ稼働中に未約定注文を1本置き、`runtime/latest_account.md` の Pending Orders 節に現れるかを見る(現状ブリッジは注文が無くても節を書くため、空欄では判定できない)。判明したら [[P40]] のステータスを更新する | 本人 + Claude(発注はClaude、判定はデータで) | I3(ブリッジ再開)の後 | 未処理 |
| **I3** | **ブリッジの再開**。`nohup python3 src/bridge/mt5_ai_bridge.py &`。玉を持たない週末は止めてよいという型(conclusions 2026-08-10)に従い停止中。**金で作戦を組むなら早めに上げる方が有利** — 帯を引くための実データが貯まるため | 本人 or 運用セッション | 月曜オープン(8/17 07:00 JST)後 | 未処理 |
| **I4** | **金(XAUUSD-m)の作戦設計**。水準は**月曜オープン後の実データで帯を引く**(金曜クローズの値で引かない — 8/10に58時間古い数字を現在値として読みかけた教訓)。**引き金は運用セッションの `--rules` に渡せる数値条件で書き、判定者を明示する**(作戦09・17・21で同型の取り逃し3例) | Claude(設計)→ 運用セッション(判定・執行) | I3の後、指標をまたぐ前 | 未処理 |
| **I5** | **米指標の市場コンセンサスを発表前日に再取得**。**Empire(8/17)・米総合PMI(8/21)・日本総合CPI(8/21)は Web検索・Codex の両系統とも取得できていない**。予想がなければ上振れ/下振れの判定ができず、指標をまたぐ作戦は組めない | Claude(Web検索 + Codex の二系統) | 各指標の前日 | 未処理 |
| **I6** | **ジャクソンホール(8/27〜29)のプログラム再確認**。日程とテーマはカンザスシティ連銀公式で確認済みだが、**「8/28に Kevin Warsh 議長の初基調講演」は未確認**(公式に講演者別プログラムが未掲載、Codexも裏取り不可)。確定事項として扱わない | Claude | 8/24週 | 未処理 |
| **I7** | **`office-work-dc`(M4環境)への到達**。`ListAgents` に現れず `SendMessage` も「No agent named 'office-work-dc' is reachable」で到達不能。**別マシンのセッションを宛先にするには、こちら側で Remote Control が接続されている必要がある**。接続はこのセッションからは操作できない。なお **MT5・EA関連のタスクはM4では実行できない**(Windows専用)ため、投げられるのはリポジトリ上で完結する作業のみ | 本人(Remote Control の有効化) | タスク管理をM4へ移すとき | 未処理 |

## 判断済み・保留にしたもの

- **EAの再コンパイルは今はやらない(2026-08-16)**: リモートアクセスのみの状況で、再コンパイル+EA再読み込みは**チャートのEA入力を既定値に戻す事故**を実際に2種類起こしている(`InpCodexMaxLot`→0.01 が8/11、`InpPollCodexTradeCommands`→false が同日2回)。**入力の復旧にはWindows GUIが要る**ため、リモートでは復旧コストが高い。I2でコンパイル済みと確認できれば、そもそも不要。
  - なお**コマンドラインからのコンパイル自体は可能**(検証済み): WSL→Windows interop 有効、`/mnt/c/Program Files/Titan FX MetaTrader 5/MetaEditor64.exe` を確認。`MetaEditor64.exe /compile:"<mq5のWindowsパス>" /log` で F7 を押さずに通せる。**必要になったときの手段としてここに残す**
