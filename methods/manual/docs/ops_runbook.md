# 運用セッション手順書(ops runbook)

軽量モデルの別セッションが、トレードの定期監視・報告・委任済み執行を担うための手順書。**相談・議論・分析はこのセッションの仕事ではない** — 求められたら「相談セッションで」と案内する。

最終更新: **2026-08-10 19:26 JST(作戦11の監視を開始。条件成立時は相談セッションへ通知)**

## 監視停止中(2026-08-08 06:3X〜)

本人指示により監視Monitorを停止した。**停止時点でポジション・注文ともにゼロ、委任執行もゼロ件**(残高381,085円)。再開時はランブック起動手順に従いブリッジ・Monitorを立ち上げ、campaign.mdで生きている作戦がないか確認してから監視を再開する。

## 見るべきものの順序

1. **この下の「現在の状態」と「イベント別対応表」** — 今やること
2. `campaign.md` — 生きている作戦とその無効化条件・トレーリング基準
3. `runtime/latest_account.md` — ライブの口座・ポジション
4. 終了した作戦・過去の対応表は **`archive/`**。**現在の判断には使わない**

## 役割の境界

- **やること**: 監視Monitorの維持、15分ダイジェスト報告、水準・指標アラート対応、**下記の委任済みルールの執行のみ**、約定・執行の campaign.md 経過記録への追記とコミット
- **やらないこと**: 新規エントリー、委任外の判断、サイズ・作戦の変更、分析・議論(→相談セッションへ)、ルールにない事態への対処(→本人へ報告して指示待ち)
- 迷ったら: **何もせず報告**。サーバー側SL/TPが最後の防衛線として常駐している

## 引き継ぎ実施記録

- 2026-08-05 20:56 JST: 運用セッション稼働開始。ブリッジhealth OK、Monitor起動(brsts8b4e)。本人確認: 相談セッション側Monitor停止済み(二重執行防止確認完了)
- **2026-08-07 21:55 JST: 作戦06(NFP後の順張り自動設置)を相談セッションから移管**。本人指示「②で引き継ぎましょう。opusの節約をしたい」。相談セッション側のMonitor(bniy43qj2)は停止済み。**以後、作戦06の設置・撤廃は運用セッションが単独で持つ**
- **2026-08-10 11:1X JST: 監視再開**。本人指示によりMonitor起動(task `bwl1fbn54`、現在保有作戦なしのため水準・指標指定なし)。相談セッション(トレードリポジトリ内の別セッション)と相互確認: Monitorはこちらの1本のみ、委任執行ゼロ件で一致。相談セッション側が15:58にブリッジ停止(金曜クローズ値のまま58時間停止)を発見・復旧済みとの報告あり、復旧後のデータ流入をこちらでも確認(server_time 2026.08.10 11:12、口座・ポジションともにNone/フラット、残高381,085円)
- **2026-08-10 19:3X JST: 作戦11の監視開始**。相談セッションから疎通確認+委任(監視・通知のみ、発注・決済は一切しない)を受け、SendMessageで往復確認済み。旧task `bwl1fbn54` を停止し、水準付き(`USDJPY-m:158.68,159.05,158.42,159.80`)で新task `br8zvc2mj` を起動。水準到達・介入観測・要人発言は都度SendMessageで相談セッションへ通知する
- **2026-08-10 19:3X JST: 委任が拡大(159.80決済・介入時撤退→確認なし可、続いて11-A押し目買いエントリーも確認なし可に追加)**。11-Aエントリー委任は「新規エントリーはいかなる場合も夜間権限外」原則(2026-08-04決定)からの明示的な例外のため、相談セッション経由の伝聞のみで実行を確約せず、**本人へ直接この場で確認を取り「間違い無いです」との回答を得た**。以後、campaign.md「11-Aエントリーの委任」節の条件・発注内容どおりに執行する
- **2026-08-10 19:4X JST: 発動条件②③の数値化・watcher再起動**。本人から「監視対象だけでこの判断はできるか」と問われ検証したところ、watcher自体は①(価格到達)のみ検知可能で②(M5下支え)③(M15 RSI)は判定ロジックを持たないことが判明。相談セッションへ報告し、判定コマンド(②: latest_snapshot_USDJPY-m.json のM5直近5本安値をゾーン判定/③: `indicators.py USDJPY-m --tf M15` のRSI14)が追加された。両コマンドを実行し動作確認済み(19:36時点 M15 RSI 78.6=③未成立、M5安値ゾーン内0本=②未成立)。旧task `br8zvc2mj` を停止し、8/12 CPI・8/14小売売上高のEVENT_WARNを追加した新task `b22vaog8c` を起動
- **2026-08-10 20:2X JST: 8/13 米PPIのEVENT_WARNを追加**。本人指示によりCPI・PPI・小売売上高の3日連続21:30指標のうち抜けていたPPIを追加。旧task `b22vaog8c` を停止し新task `bm1mad60a` を起動(水準は変更なし)
- **2026-08-10 20:47 JST: EA高頻度化(30秒→5秒)は実装・配備済みだが未コンパイル(本人判断で本日は見送り)**。作戦11は現行の「EA送信30秒→watcher 12秒」の粒度で執行される。40ロットでは30秒の空白が1pip=400円。**次にポジションがなく落ち着いたタイミングで本人がF7する予定** — このセッションからコンパイルを進言しない。もし依頼されたらポジションの有無を先に確認する(エントリー委任が生きている間の再読み込みはEA数秒〜十数秒停止・`InpPollCodexTradeCommands`false化のリスクがあるため)

## 現在の状態(2026-08-10 19:26 JST 更新)

- 口座: Titan FX 9181575。残高 **381,085円**。**ポジション・注文ともにゼロ**
- **委任執行はゼロ件**。作戦11は**設計のみで未エントリー**であり、**発注はしない**(下記)
- 本日: **158.5の壁を上抜けた**(8/6・8/7・本日10:00 と3回止められた158.52〜158.57を、11:00に158.684で突破)。NFP後の下げを完全解消
- **本人が週末にブリッジを意図的に停止**していた(ポジションなしのため)。8/10 15:58に相談セッションが再起動済み。**ダイジェストのたびに health も確認すること**

## 作戦11: USDJPY 158.5上抜け後の買い(**未エントリー・監視のみ**)

全文は `campaign.md` の「作戦11」節。**必ず読んでから監視すること**。

**委任されているもの(2026-08-10 19:28、11-Aは19:30に追加。本人へ直接確認済み)**:

| 対象 | 委任 |
|---|---|
| **159.80 到達での決済**(160手前の撤退) | **確認なしで実行してよい** |
| **158.42割れでの作戦撤回**(未エントリー時) | **確認なしで実行してよい**(19:36 追加) |
| **口先介入・実弾介入の観測時の撤退** | **確認なしで実行してよい** |
| **11-A 押し目買いのエントリー**(158.68前後) | **確認なしで実行してよい**(2026-08-10 19:30 追加。詳細は campaign.md「11-Aエントリーの委任」節) |
| 11-B 抜け確定のエントリー | **委任外**。本人の判断。**通知のみ** |
| 増ロット・ナンピン・**SLの引き下げ** | **委任外**(いかなる場合も) |

**11-Aエントリーの発動条件は3つすべて必須**(1つでも欠けたら発注しない): ①158.68±5pips(158.63〜158.73)まで押す ②M5が3本以上下支え ③**M15 RSIが70未満**(最重要・追いかけ禁止)。発注: USDJPY-m buy 40ロット・成行(買い指値不可)・SL 158.42(発注と同時必須)・TP 159.80。**1回のみ**(約定したら委任は使い切り)。

**条件②③の判定コマンド(2026-08-10 19:40、数値化・動作確認済み)**:
- ②: `python3 -c "import json; d=json.load(open('runtime/latest_snapshot_USDJPY-m.json')); bars=d['timeframes']['M5']['bars'][-5:]; hit=[b for b in bars if 158.63<=b['low']<=158.73]; print(len(hit),'本')"` → 3本以上でゾーン内なら成立
- ③: `python3 methods/manual/scripts/indicators.py USDJPY-m --tf M15` → RSI14が70未満なら成立
- 数字が境界(RSI 69.8等)のときは機械的に判定せず相談セッションへ相談する

**執行手順**: ①**発行直前に価格を再取得**する ②決済後ただちに確定損益・引き金・数字を報告 ③campaign.md へ追記

### 158.42割れ・未エントリー時の撤回手順(19:36 追加)

**上抜けが騙しだったことを意味する。作戦11の前提が消えるので撤回する。**

1. **11-A / 11-B のエントリー委任を失効させる**。以後158.68まで押しても**発注しない**
2. watcher の水準から 158.68 / 159.05 / 158.42 / 159.80 を外す
3. **本人と相談セッションへ報告** — 割れた時刻・現値・M15 RSI・経緯
4. campaign.md へ「撤回」として追記し、**作戦11をアーカイブへ移す**

**ポジション保有中に158.42へ来た場合はサーバー側SLが執行する**ので、手動決済は不要。結果の報告のみ。

**なぜ運用セッションに委任するか**: 相談セッションはMonitorを持たず、159.80に到達しても気づけない。**委任と監視は同じセッションに置く**(作戦09で分離して失敗した)。

### 監視する水準

| 水準 | 意味 | 成立したら |
|---|---|---|
| **158.68** | **11-A の押し目買い候補**(抜けた天井の上端) | **相談セッションへ通知** |
| **159.05** | **11-B の上抜け確定**(M15終値2本連続で159.05以上) | **相談セッションへ通知** |
| **158.42** | 11-A の無効化(旧天井の下) | **保有中=SLが自動執行、結果を報告 / 未エントリー=作戦11を撤回**(下記) |
| **159.80** | **160手前の撤退ライン**。ポジションを持っていれば決済提案の水準 | **相談セッションへ通知** |

### 11-A の発動条件(3つすべて。**判定は相談セッションが行う**)

1. **158.68±5pips(158.63〜158.73)まで押す**
2. **M5の直近5本のうち3本以上の安値が 158.63〜158.73 に収まる**
3. **M15 RSIが70未満**(追いかけの禁止。**最重要**)

**②③は目視でなく数値で判定する**(2026-08-10 19:40 追加):

```
python3 -c "
import json
d=json.load(open('runtime/latest_snapshot_USDJPY-m.json'))
bars=d['timeframes']['M5']['bars'][-5:]
hit=[b for b in bars if 158.63 <= b['low'] <= 158.73]
print('安値:', [f\"{b['low']:.3f}\" for b in bars], f'→ ゾーン内{len(hit)}本')
"
python3 methods/manual/scripts/indicators.py USDJPY-m --tf M15   # RSIが70未満か
```

**運用セッションは①の到達を通知するだけでよい。**②③の判定と発注可否は相談セッションと本人が決める。

### 通知の方法(2026-08-10 本人指示)

**上記の水準に到達したら、`SendMessage` で相談セッションへ飛ばす。** 宛先は下記「相談セッションの宛先」を参照。

通知に含めること: **到達した水準 / その時刻 / 現値 / M15 RSI / M5 ATR / ポジションの有無**。

**口先介入・実弾介入の観測、要人発言があった場合も同様に即通知**(水準に関わらず)。

## アーカイブへ移すルール(2026-08-08 合意)

対応表の行や委任の節が次のいずれかになったら、**その場で** `archive/ops_runbook_retired.md` へ移す。

- 対象のポジション・注文が**決済/撤去された**
- 委任が**失効・解除された**
- 参照している作戦が**終了した**(その作戦の水準・チケット番号を含む行はすべて道連れ)

**取り消し線(~~行~~)で残さない。** 無効な行が表に居座ると、生きている規定との区別がつかず、古いチケット番号や古い水準に対して行動する事故になる。移すときは失効理由を1行添える。

## 起動手順

1. ブリッジ稼働確認: `curl -s -m 3 http://127.0.0.1:8765/health`(落ちていれば `nohup python3 src/bridge/mt5_ai_bridge.py > runtime/bridge_stdout.log 2>&1 &`)
2. 監視Monitorを起動(persistent):

```
python3 -u methods/manual/scripts/runtime_watch.py --interval 12 --digest-minutes 15 --stale-seconds 240 2>&1
```

(2026-08-08 03:03 現在、保有中の作戦なしのため水準指定なし。新しい作戦が始まったら campaign.md の無効化条件・トレーリング水準に合わせて `--levels` を追加し watcher を再起動する)

**watcher更新(2026-08-06)**: 保有玉の current 価格も水準監視の価格源に使うようになった(スナップショットが銘柄交互更新で片方盲目になる穴を塞ぐ)。約定・決済・SL/TP変更は FILLED / CLOSED / SLTP_CHANGED としてチケット単位で通知される

**銘柄別ファイル(2026-08-07 追加)**: ブリッジが `runtime/latest_snapshot_<銘柄>.json` / `latest_context_<銘柄>.md` / `latest_account_<銘柄>.md` を書き出すようになった。**価格や1分足を見るときは銘柄別ファイルを使うこと**(共有の `latest_snapshot.json` は2つのEAが交互に上書きするため、片方の銘柄のバーが失われる)。watcher も銘柄別ファイルを優先して読む。**この変更を反映するには watcher の再起動が必要**

3. 起動したら本人に「運用セッション稼働開始」を報告。**相談セッション側のMonitor停止を本人経由で確認**(二重執行防止 — 両セッションが同時にルール執行すると決済・修正が二重になる)

## 日次の経済指標確認(2026-08-06 追加、本人指示)

**毎営業日の東京時間開始前(JST 9時台)に、その日の米・日の重要指標を確認して報告する**。8/6は週初に確認した ISM と NFP しか把握しておらず、23:09 の25pips急騰(SL到達・−14,680円)の原因を事前に把握できていなかった。

手順:

1. TradingView 経済カレンダー(`https://www.tradingview.com/symbols/USDJPY/economic-calendar/`)またはWeb検索で、**当日と翌日**の高重要度指標(★★★)を確認
2. 発表時刻(JST)・指標名を報告し、`runtime/ops_digest.log` に `EVENTS_TODAY` 行として記録
3. 保有玉があれば、各指標の60分前・10分前の警告を watcher の `--events` に追加(watcher再起動が必要な場合は本人に確認)
4. 米指標の定番時刻(JST): 21:30(雇用統計・CPI・小売等)/ 23:00(ISM・消費者信頼感等)/ 翌3:00(FOMC)。**日本時間22:30〜23:30は特に警戒帯**

## イベント別対応表

| イベント | 対応 |
|---|---|
| DIGEST(15分) | 1〜2行で報告(USDJPY・金・含み損益)。変化なしなら「変化なし」と一言 |
| POSITIONS 1→0 / 2→0 | 決済完了(TP2 or 建値ストップ)。deal内容を確認し報告+campaign記録。**新規注文は置かない** |
| EVENT_WARN(指標60分前/10分前) | 本人に通知。**NFP(8/7 21:30)は発表済み**。次営業日の指標は「日次の経済指標確認」節に従い改めて確認する |
| FILLED ticket / CLOSED ticket / SLTP_CHANGED | **チケット単位の変化検知**(2026-08-06 追加)。約定・決済・SL/TP変更を個別に報告する。ポジション数の増減より確実なので、**こちらを一次情報として扱う**。内容確認して報告+campaign経過記録。**短時間に連続発火する場合**(2026-08-07 追記): 本人がMT5で手動調整している可能性が高い(8/7 01:23〜01:33に10分で複数回発生した実例あり)。個別報告は抑制してよいが、**報告を完全に止めず5分ごとに集約サマリを1回出す**(最終的なSL/TP値と変更回数)。完全停止は本物の異常を見落とす |
| SYMBOL_STALE / DATA_STALE | 報告。5分以上続けばEAログ確認(`MT5ログ: ~/Library/Application Support/net.metaquotes.wine.metatrader5/drive_c/Program Files/MetaTrader 5/MQL5/Logs/`)。チャート時間足がM1以外になっていたら本人にM1復帰を依頼 |
| BALANCE 変化 | 決済損益 or 入出金。内容確認して報告 |

## チャート運用の注意(本人へ随時案内してよい)

- EA付きチャート(XAUUSD-m M1 / USDJPY-m M1)は**データ供給専用・M1固定**。時間足を変えるとEAがpassive化し監視が盲目になる(8/5に2回発生)。時間足を見たいときはEAなしの別チャートを開いてもらう

## 夜間ウォッチ

本人が就寝宣言+当夜ルールを伝えた場合のみ: campaign.md 就寝時ルール欄に記入してから、防御行動(決済・削減)に限り確認なしで実行可。新規・増ロットは不可。詳細はリポジトリ CLAUDE.md「夜間ウォッチ」節。

## 報告様式

### 末尾に必ず付ける枠(2026-08-08 追加)

**すべての状況報告の末尾に付ける。該当がなければ「なし」と書く** — 書けないなら何かある。

```
【気になる点】
- 気になること: <具体的に。なければ「なし」>
- 根拠が薄い箇所: <見立てのうち確認できていない部分>
- 判断が要る場面: <本人に決めてほしいこと。期限つきなら時刻も>
```

**次に該当したら必ず言う**(黙って進めない):

1. SL/TPが**帯の内側**にある → どの帯か、何回止められたか
2. 見ているデータが**部分的** → 「M1の60本だけ」等と明示。**上位足を見ずに方向を語らない**
3. **RRが1.2未満** → 「入らないが答え」と先に言う
4. **本人の見立てと割れている** → 数字で差を出す(黙って従わない)
5. **原則から外れる**提案をしようとしている → どの原則のどこに反するか
6. **締め切りが近い**(指標・週末クローズ) → 残り時間と、要る判断
7. 前回の報告から**前提が変わった** → 何が変わったか

**「どうされますか」で終えない**。推奨を先に出し、本人が否定だけすればよい形にする。

### 従来の報告様式

- ダイジェスト: `**定期報告(HH:MM)**: USDJPY xxx.xx(±X.Xpips/幅X.X) 含み損益±x,xxx円 / 金 x,xxx(±X.X/幅X.X) / 玉N SL:xxx.xx / 特記事項`
  - **変動の表記(2026-08-07 追加、本人要望)**: watcherのDIGEST行が `銘柄:現在値(区間の増減/幅N)` を出すようになった。**その数字をそのまま転記する**(自分で計算しない)。単位はUSDJPYがpips、金がドル。増減は前回ダイジェストからの変化、幅はその15分間の高安差
  - 幅が小さい(USDJPY 10pips未満など)なら「膠着」、大きいなら「変動中」と一言添えてよい
  - **含み損益は `runtime/latest_account.md` の Open Positions 各行の P/L を合計してそのまま転記する。pips を自分で計算しない**(2026-08-05 フォーマット改定: 軽量モデルの pips 計算で符号が反転した事故への対策。ショートは価格が建値より下=利益であり、価格差の符号と損益の符号は逆になる)
  - 玉N = ポジション数、SL: は現在のSL水準(建値なら「建値157.81」)。特記事項がなければ「特記なし」
- 執行報告: 何を・なぜ(どのルール)・結果(チケット・価格)・次の状態
- 時刻はウォッチャー出力のタイムスタンプをそのまま使う(推定時刻を書かない)
- **報告はチャットと同時に `runtime/ops_digest.log` にも1行追記する**(他セッションから `tail runtime/ops_digest.log` で参照可能にするため。2026-08-05 本人決定)。書式: `YYYY-MM-DD HH:MM:SS DIGEST USDJPY-m:xxx.xxx(±X.X/幅X.X) XAUUSD-m:xxxx.xx(±X.X/幅X.X) pos:N pl:±NNNN bal:NNNNNN note:...`(pl は Open Positions の P/L 合計、円) / 執行時は `EXEC ルール名 内容...` の行を追記

## 禁止事項(リポジトリCLAUDE.mdの心構えより)

- 事実の顔をした創作・メタ的な演出の出力(心構え11・12)
- 出典のない数値・時刻の記載
- 委任外のlive発注(発注経路の権限があっても、ルールにない操作はしない)
